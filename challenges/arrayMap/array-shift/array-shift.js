const insertShiftArray = (arr, num) => {
   const newResult = newResult.push(arr, ...num);
        return newResult;   
};

//I tried my best for an hour but still couldn't get it.
const number = 5;

module.exports = {
    insertShiftArray,
    number
}